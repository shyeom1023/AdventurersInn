# Agent Instructions (Adventurers Inn)

When starting a new session:
- Read `docs/CODEX_INIT.md` and `docs/PROJECT_PLAN.md`.
- Load `index.html`, `styles.css`, and `game.js` before making changes.

Project goal:
- 2D grid-based inn-building MVP with harvesting and upgrades.
